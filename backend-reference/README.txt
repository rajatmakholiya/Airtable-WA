The files in this folder are for reference. 
To run the full stack app:
1. Initialize a new Node project (`npm init -y`)
2. Install dependencies: `npm install express mongoose dotenv cors axios`\
4. Set up .env
5. Run `ts-node server.ts`
